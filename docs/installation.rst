============
Installation
============

This can also be installed by conda via the `conda-channel <_static/conda-channel/index.html>`_ ::

    conda install --name tmpEnv --channel https://cars-overhead-with-context.readthedocs.io//_static/conda-channel cars-overhead-with-context

At the command line::

    pip install cars-overhead-with-context
